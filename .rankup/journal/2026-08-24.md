# 2026-08-24

## 14:20Z — rankup init（已有 Vite SPA，不重建栈）

- 目标：写入项目记忆，并补上 Cloudflare Pages 部署与抓取所需的最小文件，供主 agent 部署。本会话不登录 Wrangler、不部署、不提交。
- 观察：
  - 产品：Diagrflow — focused web tool for journal-ready PRISMA 2020 flow diagrams for systematic reviews
  - 栈：Vite + React 19 + react-router-dom SPA。路由 `/` HomePage、`/editor` EditorPage、`/pricing` PricingPage。定价页与 README 写明免费 MVP、无支付
  - Git：origin `https://github.com/rambo586/diagrflow.git`，branch `main`，SHA `0524d8c`，公开仓库。提交：initial；product (#1)；Cloudflare Pages SPA fallback `public/_redirects` (#2)。无 GitHub Actions，当时无 wrangler.toml、无 sitemap/robots
  - 测试 / 构建：`npm test`（vitest）；`npm run build`（`tsc --noEmit && vite build`）
  - `index.html` 已有 title/description/canonical/OG 文案，无 og:image。`public/_redirects`：`/* /index.html 200`。`public/favicon.svg` 存在
  - 域 `diagrflow.com`：NS 已是 `noel.ns.cloudflare.com` / `alina.ns.cloudflare.com`；A 记录为 Cloudflare 代理 IP；TLS 证书存在（Google Trust Services WE1，签发 2026-08-24）。HTTPS HEAD 超时（0 bytes）。HTTP 200 带 `x-amz-version-id` — 停车/占位，不是本应用。Zone `active`，id `c27533790b69e929fbbf85d160269613`（CF API，同日；未登录 Wrangler）
  - Wrangler 4.125.0 经 `opencli wrangler` 可用；whoami：NOT authenticated
  - `.rankup/skill-state.json` 已在（installed 2.43.0，latest 2.48.0 blocked as source-checkout）。未覆盖该文件
  - 生命周期：阶段 3 完成；阶段 4 zone 已在 CF 但无 Pages/Worker；阶段 5 MVP 存在；阶段 6 无 Stripe（有意）；当前阶段 7。阶段 1 关键词未调研
- 操作：按 `project-memory.md` 创建 wiki 全套；添加 `wrangler.jsonc`、`public/robots.txt`、`public/sitemap.xml`、`public/_headers`、`public/llms.txt`；更新 `package.json` 脚本与 wrangler devDependency、`.gitignore`
- 验证：本条目写入时部署未跑。测试/构建结果见主 agent 回收的命令输出
- 决定：见 DEC-20260824-01..04（保留 SPA；Pages 静态；无支付；生产域 diagrflow.com）
- 经验：无跨日复用结论；域名在 CF 上 ≠ 源站是我们的应用
- 后续：用户完成本机 Wrangler 登录后，主 agent 执行 `npm run deploy`，绑定 `diagrflow.com`，验证三路由
- 关联：INDEX.md、plan.md、infrastructure.md、decisions.md

## 14:35Z — 首次生产部署

- 目标：把 SPA 放到 Cloudflare Pages，并把 `diagrflow.com` 从停车页切到本应用
- 观察：Wrangler OAuth 成功（`bo5861142@gmail.com`）。Pages 自定义域报 `CNAME record not set`。DNS 上有两条停车 A：`54.149.79.189`、`34.216.117.25`（Spaceship）。OAuth 无 DNS 写权限，改在控制台删 A、加 CNAME。
- 操作：`wrangler pages project create diagrflow`；`wrangler pages deploy dist`（部署 id `28277423`）；API 添加 `diagrflow.com` 与 `www.diagrflow.com`；DNS CNAME apex+www → `diagrflow.pages.dev` Proxied
- 验证：`https://diagrflow.com/` 200 + Diagrflow title；浏览器首页 H1；`/editor?sample=1` 26 inputs / Download SVG / screened 1278；robots/sitemap/llms 200。www 公开 DNS 未确认。
- 决定：保持静态 Pages，不迁 Workers
- 经验：同账号 zone ≠ 自动 CNAME。停车 A 会挡住 Pages 自定义域。
- 后续：阶段 7.5
- 关联：releases.md、iterations.md 迭代 1
